# 提肛计数应用 - 部署方案

**版本**: V1.0  
**日期**: 2025-12-04 12:03  
**作者**: AI Assistant

---

## 一、方案概述

本项目为纯前端静态应用（Vite + React + TypeScript），可使用免费静态网站托管服务部署。

### 推荐平台对比

| 平台 | 月费用 | 带宽限制 | 构建时间 | 全球CDN | 自定义域名 |
|------|--------|----------|----------|---------|-----------|
| **Vercel** ⭐ | 免费 | 100GB/月 | 6000分钟/月 | ✅ | ✅免费 |
| **Cloudflare Pages** | 免费 | 无限制 | 500次/月 | ✅ | ✅免费 |
| **GitHub Pages** | 免费 | 100GB/月 | 自行配置 | ✅ | ✅免费 |
| **Netlify** | 免费 | 100GB/月 | 300分钟/月 | ✅ | ✅免费 |

---

## 二、方案一：Vercel（⭐首选推荐）

### 2.1 优势
- 零配置部署，自动识别 Vite 项目
- 每次 Git 推送自动部署
- 免费 SSL 证书
- 全球边缘网络加速
- 预览部署（PR 自动生成预览链接）

### 2.2 部署步骤

#### 步骤1：准备代码仓库
```bash
# 在项目根目录初始化 Git
cd /Users/david/Desktop/pitem/提刚V2.0
git init
git add .
git commit -m "Initial commit: 提肛计数应用"

# 在 GitHub 创建仓库后推送
git remote add origin https://github.com/你的用户名/kegel-trainer.git
git branch -M main
git push -u origin main
```

#### 步骤2：连接 Vercel
1. 访问 [vercel.com](https://vercel.com)
2. 使用 GitHub 账号登录
3. 点击 "Add New Project"
4. 选择刚创建的仓库 `kegel-trainer`
5. Vercel 会自动检测 Vite 配置

#### 步骤3：配置部署
Vercel 会自动检测，通常无需修改：
- **Framework Preset**: Vite
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`

#### 步骤4：点击 Deploy
等待约1-2分钟，部署完成后获得免费域名：
```
https://你的项目名.vercel.app
```

### 2.3 自定义域名（可选）
1. 在 Vercel 项目设置 → Domains
2. 添加你的域名
3. 按提示配置 DNS 记录

---

## 三、方案二：Cloudflare Pages

### 3.1 优势
- 无限带宽
- 全球最快的 CDN 网络
- 免费 DDoS 防护
- 构建速度快

### 3.2 部署步骤

#### 步骤1：准备代码仓库（同上）

#### 步骤2：连接 Cloudflare
1. 访问 [pages.cloudflare.com](https://pages.cloudflare.com)
2. 登录/注册 Cloudflare 账号
3. 点击 "Create a project" → "Connect to Git"
4. 授权并选择仓库

#### 步骤3：配置构建
- **Framework preset**: 选择 "None"
- **Build command**: `npm run build`
- **Build output directory**: `dist`
- **Root directory**: `/`（留空）

#### 步骤4：部署
完成后获得免费域名：
```
https://你的项目名.pages.dev
```

---

## 四、方案三：GitHub Pages（免费+稳定）

### 4.1 优势
- 与 GitHub 深度集成
- 完全免费，无限制
- 最稳定的服务

### 4.2 部署步骤

#### 步骤1：修改 vite.config.ts
```typescript
export default defineConfig({
  base: '/仓库名/',  // 添加这行，替换为你的仓库名
  plugins: [react()],
  // ... 其他配置
});
```

#### 步骤2：创建 GitHub Actions 工作流
创建文件 `.github/workflows/deploy.yml`:
```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - uses: actions/deploy-pages@v4
```

#### 步骤3：启用 GitHub Pages
1. 仓库 Settings → Pages
2. Source 选择 "GitHub Actions"
3. 推送代码触发部署

完成后访问：
```
https://你的用户名.github.io/仓库名/
```

---

## 五、成本对比总结

| 方案 | 初始成本 | 月度成本 | 年度成本 |
|------|----------|----------|----------|
| Vercel | ¥0 | ¥0 | ¥0 |
| Cloudflare Pages | ¥0 | ¥0 | ¥0 |
| GitHub Pages | ¥0 | ¥0 | ¥0 |
| 自定义域名（可选） | ¥30-80/年 | - | ¥30-80 |

**结论：三种方案都完全免费！**

---

## 六、最终推荐

### 🏆 最佳选择：Vercel

**理由：**
1. 部署最简单，5分钟完成
2. 自动检测 Vite 项目，零配置
3. 每次 push 自动更新
4. 免费 HTTPS + 全球 CDN
5. 国内访问速度尚可

### 🥈 次选：Cloudflare Pages

**理由：**
1. 无限带宽，不限流量
2. 全球最快的 CDN
3. 如果有自己域名，推荐使用

---

## 七、快速开始命令

### 本地构建测试
```bash
cd /Users/david/Desktop/pitem/提刚V2.0
npm run build    # 构建生产版本
npm run preview  # 本地预览生产版本
```

### 推送到 GitHub
```bash
git init
git add .
git commit -m "feat: 提肛计数应用 v1.0"
git remote add origin https://github.com/你的用户名/kegel-trainer.git
git push -u origin main
```

然后在 Vercel 导入仓库即可自动部署！

---

## 八、注意事项

1. **首次部署**：确保本地 `npm run build` 能成功执行
2. **环境变量**：本项目无需配置环境变量
3. **域名备案**：如果使用国内域名，需要备案；使用 `.vercel.app` 或 `.pages.dev` 免备案
4. **访问速度**：对于国内用户，Vercel 和 Cloudflare 速度都不错

---

**版本历史**
| 版本 | 日期 | 更新内容 |
|------|------|----------|
| V1.0 | 2025-12-04 | 初始版本，包含三种免费部署方案 |

